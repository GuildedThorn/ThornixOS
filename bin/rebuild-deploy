#!/usr/bin/env bash

PRIMARY_USER="$1"
HOST="$2"

printf '%s\n' "$PRIMARY_USER" | sudo tee ../../nixos/current-user.lock >/dev/null
printf '%s\n' "$HOST" | sudo tee ../../nixos/current-host.lock >/dev/null

cd ././nixos
sudo nixos-rebuild switch --flake ./".#${HOST}" --upgrade
